export default function FormElement({ element }) { const fields = element.fields || [{ label: 'Name', type: 'text' }, { label: 'Email', type: 'email' }, { label: 'Message', type: 'textarea' }]; const input = { display: 'block', width: '100%', marginBottom: 10, padding: 10, borderRadius: 7, border: '1px solid rgba(255,255,255,.18)', background: 'rgba(255,255,255,.1)', color: '#fff' }; return <form className="sm-form-element" style={{ width: '100%', height: '100%' }} onSubmit={(event) => event.preventDefault()}>{fields.map((field) => field.type === 'textarea' ? <textarea style={{ ...input, minHeight: 55 }} key={field.label} placeholder={field.label} disabled /> : <input style={input} key={field.label} type={field.type} placeholder={field.label} disabled />)}<button style={{ border: 0, borderRadius: 999, padding: '9px 15px', background: '#fff', color: '#111827', fontWeight: 700 }} type="submit">Send message</button></form>; }
