'use client';

import { useEffect, useMemo, useState } from 'react';
import Link from 'next/link';
import GoogleAuthButton from '../../components/GoogleAuthButton';
import { convertToSRT } from '../../lib/convertToSRT';

const GENRES = ['Horror', 'Comedy', 'Educational', 'Drama', 'Motivational', 'Documentary', 'Kids Story', 'Other'];
const STYLES = ['Anime', 'Realistic/Cinematic', 'Flat Illustration', '3D Render', 'Documentary Photography'];
const MOODS = ['Dark & Gritty', 'Uplifting', 'Suspenseful', 'Emotional', 'Adventurous', 'Lighthearted', 'Mysterious', 'Romantic', 'Intense'];
const TONES = ['Cinematic', 'Literary', 'Casual', 'Poetic', 'Thriller Style'];
const CHARACTER_ROLES = ['Hero', 'Villain', 'Anti-hero', 'Sidekick', 'Love Interest', 'Mentor', 'Detective', 'Survivor'];
const PERIODS = ['Ancient', 'Medieval', 'Victorian', '1900s', 'Modern', 'Near Future', 'Far Future', 'Alternate Reality'];
const PLACES = ['City', 'Village', 'Space', 'Forest', 'Ocean', 'Mountains', 'Abandoned place', 'Castle', 'Dystopia', 'Spaceship', 'Post-apocalypse', 'Fantasy world'];
const WORLDS = ['Realistic', 'Magical', 'Futuristic', 'Dystopian', 'Mythological', 'Parallel Universe'];
const CONFLICTS = ['Person vs person', 'Person vs nature', 'Person vs society', 'Person vs self', 'Person vs unknown', 'Person vs love'];
const TWISTS = ['Betrayal', 'Hidden identity', 'Time travel', 'Revenge', 'Sacrifice', 'Escape', 'Reunion', 'Big reveal', 'Secret past'];
const PACES = { Slow: 2, Normal: 2.5, Fast: 3.5 };
const PRESETS = [15, 30, 60, 180, 300];
const newScene = (number, duration = 8) => ({ id: crypto.randomUUID(), sceneNumber: number, narration: 'Add narration for this scene.', visualPrompt: 'Describe the subject, camera, lighting, colour palette, motion, environment, and texture.', estimatedDurationSeconds: duration });

function sceneCountFor(duration, pace) {
  const secondsPerScene = pace === 'Slow' ? 10 : pace === 'Fast' ? 6 : 8;
  return Math.min(12, Math.max(1, Math.round(duration / secondsPerScene)));
}
function wordTargetFor(duration, pace) { return Math.round(duration * (PACES[pace] || PACES.Normal)); }
function wordsIn(text) { return (text || '').trim().split(/\s+/).filter(Boolean).length; }
function slug(value) { return (value || 'script-writer').toLowerCase().trim().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, ''); }
function download(name, type, content) { const url = URL.createObjectURL(new Blob([content], { type })); const link = document.createElement('a'); link.href = url; link.download = name; link.click(); URL.revokeObjectURL(url); }

export default function StoryClient({ maintenanceMode = false }) {
  const [topic, setTopic] = useState(''); const [genre, setGenre] = useState('Educational'); const [format, setFormat] = useState('Shorts / Reels');
  const [duration, setDuration] = useState(60); const [customDuration, setCustomDuration] = useState(''); const [pace, setPace] = useState('Normal');
  const [pov, setPov] = useState('Third-person narrator'); const [language, setLanguage] = useState('English'); const [visualStyle, setVisualStyle] = useState('Realistic/Cinematic');
  const [mood, setMood] = useState(''); const [tone, setTone] = useState(''); const [characterRole, setCharacterRole] = useState(''); const [period, setPeriod] = useState(''); const [place, setPlace] = useState(''); const [world, setWorld] = useState(''); const [conflict, setConflict] = useState(''); const [twist, setTwist] = useState('');
  const [sceneCount, setSceneCount] = useState(sceneCountFor(60, 'Normal')); const [sceneCountEdited, setSceneCountEdited] = useState(false); const [wordTarget, setWordTarget] = useState(wordTargetFor(60, 'Normal')); const [wordTargetEdited, setWordTargetEdited] = useState(false);
  const [scenes, setScenes] = useState([]); const [title, setTitle] = useState(''); const [loading, setLoading] = useState(false); const [error, setError] = useState(''); const [notice, setNotice] = useState('');
  const [auth, setAuth] = useState(false); const [quota, setQuota] = useState(null); const [dragging, setDragging] = useState(null);
  const activeDuration = customDuration ? Math.min(300, Math.max(1, Number(customDuration) || duration)) : duration;
  const suggestedWordTarget = wordTargetFor(activeDuration, pace);
  const activeWordTarget = wordTargetEdited ? wordTarget : suggestedWordTarget;
  const wordsPerSecond = (activeWordTarget / activeDuration).toFixed(1);
  const sensibleMinimumWords = Math.max(10, Math.round(activeDuration * 1.5));
  const sensibleMaximumWords = Math.min(1050, Math.round(activeDuration * 4));
  const wordTargetIssue = wordTargetEdited && (activeWordTarget < sensibleMinimumWords || activeWordTarget > sensibleMaximumWords);
  const totalDuration = scenes.reduce((total, scene) => total + (Number(scene.estimatedDurationSeconds) || 0), 0);

  useEffect(() => { let alive = true; fetch('/api/ai-quota?tool=story-generator', { cache: 'no-store' }).then(r => r.json().then(data => ({ r, data }))).then(({ r, data }) => { if (alive) { setAuth(r.ok); setQuota(data.quota || null); } }).catch(() => {}); return () => { alive = false; }; }, []);
  const update = (id, patch) => setScenes(items => items.map(item => item.id === id ? { ...item, ...patch } : item));
  const reorder = (from, to) => { if (from === to || to === null) return; setScenes(items => { const next = [...items]; const [moved] = next.splice(from, 1); next.splice(to, 0, moved); return next.map((scene, index) => ({ ...scene, sceneNumber: index + 1 })); }); };
  const duplicateScene = index => setScenes(items => { const next = [...items]; const source = next[index]; next.splice(index + 1, 0, { ...source, id: crypto.randomUUID() }); return next.map((scene, sceneIndex) => ({ ...scene, sceneNumber: sceneIndex + 1 })); });
  const generate = async () => {
    if (maintenanceMode || loading) return;
    if (!topic.trim()) { setNotice(''); setError('Add a short topic or premise to generate your script.'); return; }
    if (wordTargetIssue) { setNotice(''); setError(`Choose a narration target between ${sensibleMinimumWords} and ${sensibleMaximumWords} words for a ${activeDuration}-second video, or use the recommendation.`); return; }
    if (!auth) { setError(''); setNotice('Sign in with Google to generate a script and access your daily free generations.'); return; }
    if (quota && quota.remaining <= 0) { setError('Your daily generation limit has been reached.'); return; }
    setLoading(true); setError(''); setNotice(''); setScenes([]);
    try {
      const response = await fetch('/api/generate-story', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ topic, genre, format, duration: activeDuration, targetWords: activeWordTarget, pace: pace.toLowerCase(), pov, language, visualStyle, mood, tone, characterRole, period, place, world, conflict, twist, sceneCount: sceneCountEdited ? sceneCount : sceneCountFor(activeDuration, pace) }) });
      const data = await response.json(); if (!response.ok) throw new Error(data.error || 'Generation failed.');
      setScenes((data.scenes || []).map((scene, index) => ({ ...scene, id: crypto.randomUUID(), sceneNumber: index + 1 })));
      setTitle(data.title || `${topic.trim()} — Script`); setQuota(data.quota || quota);
    } catch (err) { if (err.message === 'Sign in with Google to use this tool') { setAuth(false); setError(''); setNotice('Sign in with Google to generate a script and access your daily free generations.'); } else setError(err.message); } finally { setLoading(false); }
  };
  const downloads = useMemo(() => ({
    narration: scenes.map(scene => scene.narration.trim()).filter(Boolean).join('\n\n'),
    visuals: scenes.map((scene, index) => `SCENE ${index + 1}\n${scene.visualPrompt.trim()}`).join('\n\n'),
    srt: convertToSRT(scenes.reduce((result, scene) => { const start = result.elapsed; const end = start + Math.max(.1, Number(scene.estimatedDurationSeconds) || 0); result.elapsed = end; result.items.push({ start_time: start, end_time: end, narration: scene.narration }); return result; }, { elapsed: 0, items: [] }).items),
  }), [scenes]);

  return <main className="sw-root"><style suppressHydrationWarning dangerouslySetInnerHTML={{ __html: styles + advancedStyles + workspaceStyles + sceneEditorStyles }} />
    <header className="sw-header"><Link href="/" className="sw-brand"><span>✦</span> ToolVoid <i>/</i> Script Writer</Link><GoogleAuthButton compact /></header>
    <section className="sw-hero"><div className="sw-eyebrow">AI VIDEO PRE-PRODUCTION</div><h1>Write the script.<br/><em>Direct every frame.</em></h1><p>One production-ready brief creates narration, standalone AI visual prompts, and timed captions — scene by scene.</p></section>
    {maintenanceMode && <div className="sw-maintenance"><strong>Script Writer is under maintenance.</strong> The rebuilt workspace is ready for testing; generation remains disabled until it is taken live.</div>}
    <div className={`sw-shell ${maintenanceMode ? 'is-locked' : ''}`} aria-disabled={maintenanceMode} inert={maintenanceMode || undefined}>
      <aside className="sw-panel"><div className="sw-panel-head"><span>01</span><div><b>Production brief</b><small>Set the creative constraints</small></div></div>
        <div className="sw-control-group"><div className="sw-group-label">Story foundations</div><label className="sw-field wide"><span>Topic / premise</span><textarea value={topic} onChange={e => setTopic(e.target.value)} placeholder="e.g. The surprising science behind why octopuses change colour" maxLength={350}/><small>{topic.length}/350 · Keep it concise</small></label><div className="sw-grid"><label className="sw-field"><span>Genre / tone</span><select value={genre} onChange={e => setGenre(e.target.value)}>{GENRES.map(x => <option key={x}>{x}</option>)}</select></label><label className="sw-field"><span>Format</span><select value={format} onChange={e => setFormat(e.target.value)}><option>Shorts / Reels</option><option>Long-form</option></select></label></div></div>
        <div className="sw-control-group"><div className="sw-group-label">Timing & voice</div><div className="sw-field"><span>Target duration</span><div className="sw-pills">{PRESETS.map(x => <button key={x} className={!customDuration && duration === x ? 'active' : ''} onClick={() => { setDuration(x); setCustomDuration(''); }}>{x < 60 ? `${x}s` : x < 180 ? '60s' : `${x / 60}min`}</button>)}<input aria-label="Custom duration in seconds" type="number" min="1" max="300" value={customDuration} onChange={e => setCustomDuration(e.target.value)} placeholder="Custom"/></div><small>Maximum 5 minutes to keep narration and visual direction within one generation.</small></div><div className="sw-grid"><label className="sw-field"><span>Speaking pace</span><select value={pace} onChange={e => setPace(e.target.value)}>{Object.keys(PACES).map(x => <option key={x}>{x}</option>)}</select></label><label className="sw-field"><span>Narration POV</span><select value={pov} onChange={e => setPov(e.target.value)}><option>First-person narrator</option><option>Third-person narrator</option><option>Character dialogue</option></select></label></div><label className="sw-field"><span>Language</span><select value={language} onChange={e => setLanguage(e.target.value)}><option>English</option><option>Hindi</option><option>Hinglish</option></select></label><label className="sw-field"><span>Narration word target <small>{wordTargetEdited ? 'Custom' : 'Recommended'}</small></span><div className="sw-word-target"><input aria-label="Narration word target" type="number" min="10" max="1050" value={activeWordTarget} onChange={e => { setWordTargetEdited(true); setWordTarget(Math.min(1050, Math.max(10, Number(e.target.value) || 10))); }}/><b>words</b><button type="button" onClick={() => { setWordTargetEdited(false); setWordTarget(suggestedWordTarget); }}>Use {suggestedWordTarget}</button></div><small>{activeDuration}s at this target is about {wordsPerSecond} words per second. Adjust it for a denser or more spacious voiceover.</small>{wordTargetIssue && <p className="sw-word-warning" role="alert">This target is unlikely to fit {activeDuration}s naturally. Choose {sensibleMinimumWords}–{sensibleMaximumWords} words, or use the recommendation.</p>}</label></div>
        <div className="sw-control-group"><div className="sw-group-label">Visual continuity</div><label className="sw-field"><span>Visual style</span><select value={visualStyle} onChange={e => setVisualStyle(e.target.value)}>{STYLES.map(x => <option key={x}>{x}</option>)}</select></label><label className="sw-field"><span>Scene count <small>Auto-calculated · editable</small></span><input type="number" min="1" max="12" value={sceneCountEdited ? sceneCount : sceneCountFor(activeDuration, pace)} onChange={e => { setSceneCountEdited(true); setSceneCount(Math.min(12, Math.max(1, Number(e.target.value) || 1))); }}/><small>About {Math.round(activeDuration / (sceneCountEdited ? sceneCount : sceneCountFor(activeDuration, pace)))} seconds per scene at {pace.toLowerCase()} pace.</small></label></div>
        <details className="sw-advanced"><summary>Optional story details <span>+</span></summary><p>Carry over the old Story Generator&apos;s structured creative controls when your video needs a more specific world, character, or plot arc.</p><div className="sw-grid"><label className="sw-field"><span>Mood</span><select value={mood} onChange={e => setMood(e.target.value)}><option value="">Auto</option>{MOODS.map(x => <option key={x}>{x}</option>)}</select></label><label className="sw-field"><span>Writing tone</span><select value={tone} onChange={e => setTone(e.target.value)}><option value="">Auto</option>{TONES.map(x => <option key={x}>{x}</option>)}</select></label><label className="sw-field"><span>Character role</span><select value={characterRole} onChange={e => setCharacterRole(e.target.value)}><option value="">None</option>{CHARACTER_ROLES.map(x => <option key={x}>{x}</option>)}</select></label><label className="sw-field"><span>Time period</span><select value={period} onChange={e => setPeriod(e.target.value)}><option value="">Auto</option>{PERIODS.map(x => <option key={x}>{x}</option>)}</select></label><label className="sw-field"><span>Setting</span><select value={place} onChange={e => setPlace(e.target.value)}><option value="">Auto</option>{PLACES.map(x => <option key={x}>{x}</option>)}</select></label><label className="sw-field"><span>World type</span><select value={world} onChange={e => setWorld(e.target.value)}><option value="">Auto</option>{WORLDS.map(x => <option key={x}>{x}</option>)}</select></label><label className="sw-field"><span>Conflict</span><select value={conflict} onChange={e => setConflict(e.target.value)}><option value="">Auto</option>{CONFLICTS.map(x => <option key={x}>{x}</option>)}</select></label><label className="sw-field"><span>Plot twist</span><select value={twist} onChange={e => setTwist(e.target.value)}><option value="">None</option>{TWISTS.map(x => <option key={x}>{x}</option>)}</select></label></div></details>
        <div className={`sw-tries ${!auth ? 'yellow' : quota?.remaining <= 0 ? 'red' : quota?.remaining === 1 ? 'yellow' : 'green'}`}>{!auth ? 'Sign in to unlock 3 daily generations' : quota ? <><strong>{quota.used} of {quota.limit} used today</strong><small>{quota.remaining} remaining · Resets {quota.reset}</small></> : 'Loading daily free generations…'}</div>
        {notice && <p className="sw-notice"><span>✦</span>{notice}</p>}{error && <p className="sw-error">{error}</p>}<button className="sw-generate" onClick={generate} disabled={loading}>{loading ? 'Building your production script…' : 'Generate script →'}</button>
      </aside>
      <section className="sw-canvas"><div className="sw-canvas-head"><div><span className="sw-kicker">02 · SCRIPT CANVAS</span><h2>{title || 'Your scene list'}</h2><p>{scenes.length ? `${scenes.length} scenes · ${totalDuration.toFixed(0)} seconds estimated` : 'Generate a production-ready script from your brief.'}</p></div>{scenes.length > 0 && <div className="sw-export"><button onClick={() => download(`${slug(title)}-narration.txt`, 'text/plain;charset=utf-8', downloads.narration)}>Narration .txt</button><button onClick={() => download(`${slug(title)}-visual-prompts.txt`, 'text/plain;charset=utf-8', downloads.visuals)}>Visual prompts .txt</button><button onClick={() => download(`${slug(title)}-captions.srt`, 'text/plain;charset=utf-8', downloads.srt)}>Captions .srt</button></div>}</div>
        {!scenes.length && !loading && <div className="sw-empty"><div>✦</div><b>Your production script will appear here</b><p>Every scene keeps voiceover, visual direction, and captions cleanly separated.</p></div>}
        {loading && <div className="sw-empty"><div className="sw-spinner"/><b>Planning scenes and visual continuity…</b><p>One structured AI call is creating your full script.</p></div>}
        <div className="sw-scenes">{scenes.map((scene, index) => <article key={scene.id} className="sw-scene" draggable onDragStart={() => setDragging(index)} onDragOver={e => e.preventDefault()} onDrop={() => { reorder(dragging, index); setDragging(null); }}><button className="sw-drag" title="Drag to reorder" aria-label={`Drag scene ${index + 1} to reorder`}>⠿</button><div className="sw-number">{String(index + 1).padStart(2, '0')}</div><div className="sw-scene-body"><div className="sw-scene-top"><span>SCENE {index + 1}</span><div className="sw-scene-meta"><b>{wordsIn(scene.narration)} words</b><label>EST. <input aria-label={`Scene ${index + 1} duration in seconds`} type="number" min="0.1" step=".5" value={scene.estimatedDurationSeconds} onChange={e => update(scene.id, { estimatedDurationSeconds: Number(e.target.value) })}/> sec</label></div></div><label className="sw-edit narration"><span>NARRATION · TTS READY</span><textarea aria-label={`Scene ${index + 1} narration`} value={scene.narration} onChange={e => update(scene.id, { narration: e.target.value })} placeholder="Write the exact voiceover for this scene."/></label><label className="sw-edit visual"><span>VISUAL DIRECTION · GENERATOR READY</span><textarea aria-label={`Scene ${index + 1} visual direction`} value={scene.visualPrompt} onChange={e => update(scene.id, { visualPrompt: e.target.value })} placeholder="Describe the shot, subject, light, movement, and setting."/></label><div className="sw-scene-footer"><div className="sw-caption"><span>CC</span>{scene.narration || 'Caption follows narration'}</div><div className="sw-scene-actions"><button type="button" onClick={() => duplicateScene(index)}>Duplicate</button><button type="button" className="danger" onClick={() => setScenes(items => items.filter(x => x.id !== scene.id).map((x, i) => ({ ...x, sceneNumber: i + 1 })))}>Remove</button></div></div></div></article>)}</div>
        {scenes.length > 0 && <button className="sw-add" onClick={() => setScenes(items => {
          const sceneDuration = Math.max(3, Math.round(activeDuration / (items.length + 1)));
          return [...items, newScene(items.length + 1, sceneDuration)];
        })}>+ Add scene</button>}
      </section>
    </div>
  </main>;
}

const styles = `@import url('https://fonts.googleapis.com/css2?family=DM+Mono:wght@400;500&family=Syne:wght@500;600;700;800&display=swap');.sw-root{--a:#a78bfa;--p:#11111a;--line:rgba(255,255,255,.09);--t:#f5f3ff;--m:#9b98ac;min-height:100vh;background:radial-gradient(ellipse 65% 42% at 75% -5%,rgba(124,58,237,.19),transparent 72%),#09090f;color:var(--t);font-family:system-ui,sans-serif}.sw-root *{box-sizing:border-box}.sw-header{height:64px;display:flex;align-items:center;justify-content:space-between;padding:0 max(20px,calc((100vw - 1240px)/2));border-bottom:1px solid var(--line);background:rgba(9,9,15,.7);backdrop-filter:blur(18px)}.sw-brand{color:var(--t);text-decoration:none;font:700 .88rem Syne,sans-serif}.sw-brand span{display:inline-grid;place-items:center;width:25px;height:25px;border-radius:7px;background:linear-gradient(135deg,#c4b5fd,#7c3aed);margin-right:7px}.sw-brand i{font-style:normal;color:#625d70;margin:0 6px}.sw-hero{max-width:1240px;margin:auto;padding:78px 20px 48px}.sw-eyebrow,.sw-kicker,.sw-group-label{font:500 .7rem 'DM Mono',monospace;letter-spacing:.12em;color:#c4b5fd}.sw-hero h1{margin:12px 0;font:800 clamp(2.7rem,6vw,5.3rem)/.96 Syne,sans-serif;letter-spacing:-.07em}.sw-hero em{font-style:normal;background:linear-gradient(100deg,#ddd6fe,#a78bfa 50%,#f0abfc);-webkit-background-clip:text;color:transparent}.sw-hero p{max-width:600px;color:var(--m);font-size:1rem;line-height:1.7}.sw-maintenance{max-width:1240px;margin:0 auto 18px;padding:14px 18px;border:1px solid rgba(167,139,250,.4);border-radius:12px;background:rgba(167,139,250,.1);color:#ddd6fe;font-size:.86rem}.sw-maintenance strong{margin-right:8px}.sw-shell{max-width:1240px;margin:0 auto;padding:0 20px 80px;display:grid;grid-template-columns:360px minmax(0,1fr);gap:16px}.sw-panel,.sw-canvas{border:1px solid var(--line);border-radius:16px;background:rgba(17,17,26,.88);box-shadow:0 16px 45px rgba(0,0,0,.16)}.sw-panel{height:max-content;padding:20px}.sw-panel-head{display:flex;gap:10px;align-items:center;margin-bottom:19px}.sw-panel-head>span{display:grid;place-items:center;width:31px;height:31px;border-radius:9px;background:rgba(167,139,250,.16);color:#c4b5fd;font:500 .69rem 'DM Mono'}.sw-panel-head b{font:700 .94rem Syne}.sw-panel-head small,.sw-field small{display:block;color:var(--m);font-size:.7rem;margin-top:3px}.sw-control-group{padding:16px 0 1px;border-top:1px solid var(--line)}.sw-control-group:first-of-type{padding-top:0;border-top:0}.sw-group-label{margin-bottom:12px;color:#8f89a7;font-size:.62rem}.sw-field{display:block;margin:0 0 15px}.sw-field>span{display:flex;justify-content:space-between;color:#c8c5d3;font:700 .66rem 'DM Mono';letter-spacing:.07em;text-transform:uppercase;margin-bottom:7px}.sw-field>span small{margin:0;text-transform:none;letter-spacing:0}.sw-field textarea,.sw-field select,.sw-field input,.sw-edit textarea{width:100%;color:var(--t);background:#0c0c14;border:1px solid var(--line);border-radius:9px;padding:10px;font:inherit;outline:0;transition:border-color .18s,background .18s}.sw-field textarea:focus,.sw-field select:focus,.sw-field input:focus,.sw-edit textarea:focus{border-color:rgba(167,139,250,.72);background:#10101a}.sw-field textarea{min-height:84px;resize:vertical;line-height:1.45}.sw-field select{height:42px}.sw-field input{height:42px}.sw-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.sw-pills{display:flex;gap:5px;flex-wrap:wrap}.sw-pills button{padding:8px 8px;border-radius:7px;border:1px solid var(--line);background:#0c0c14;color:var(--m);font:600 .69rem 'DM Mono';cursor:pointer;transition:all .18s}.sw-pills button:hover{border-color:rgba(167,139,250,.5);color:#ddd6fe}.sw-pills button.active{color:#e9e4ff;border-color:rgba(167,139,250,.65);background:rgba(167,139,250,.16)}.sw-pills input{width:74px;height:33px;padding:5px;font-size:.72rem}.sw-tries{margin:5px 0 10px;text-align:center;font:500 .72rem 'DM Mono';padding:10px;border-radius:8px;border:1px solid var(--line);background:#0c0c14}.sw-tries.green{color:#6ee7b7;border-color:rgba(52,211,153,.22);background:rgba(52,211,153,.05)}.sw-tries.yellow{color:#fcd34d;border-color:rgba(251,191,36,.22);background:rgba(251,191,36,.05)}.sw-tries.red{color:#fca5a5;border-color:rgba(248,113,113,.22);background:rgba(248,113,113,.05)}.sw-generate{width:100%;border:0;border-radius:10px;padding:13px;background:linear-gradient(135deg,#c4b5fd,#8b5cf6);color:#190d2d;font:800 .84rem Syne;cursor:pointer;box-shadow:0 7px 20px rgba(124,58,237,.22);transition:transform .18s,opacity .18s}.sw-generate:hover:not(:disabled){transform:translateY(-1px)}.sw-generate:disabled{opacity:.55}.sw-notice,.sw-error{display:flex;gap:8px;align-items:flex-start;margin:0 0 10px;padding:10px;border-radius:8px;font-size:.75rem;line-height:1.5}.sw-notice{border:1px solid rgba(167,139,250,.24);background:rgba(167,139,250,.08);color:#d8d1f4}.sw-notice span{color:#c4b5fd}.sw-error{background:rgba(248,113,113,.1);border:1px solid rgba(248,113,113,.22);color:#fca5a5}.sw-canvas{padding:24px;min-height:650px}.sw-canvas-head{display:flex;justify-content:space-between;gap:16px;padding-bottom:19px;border-bottom:1px solid var(--line)}.sw-canvas h2{margin:6px 0 4px;font:700 1.35rem Syne;letter-spacing:-.04em}.sw-canvas-head p{margin:0;color:var(--m);font-size:.78rem}.sw-export{display:flex;align-items:flex-start;gap:6px;flex-wrap:wrap}.sw-export button,.sw-add{border:1px solid var(--line);background:#1b1928;color:#ddd6fe;border-radius:8px;padding:8px 9px;font:700 .66rem 'DM Mono';cursor:pointer;transition:all .18s}.sw-export button:hover,.sw-add:hover{border-color:#a78bfa;background:rgba(167,139,250,.12)}.sw-empty{min-height:435px;display:grid;place-content:center;text-align:center;gap:8px;color:var(--m);border:1px dashed rgba(167,139,250,.16);border-radius:12px;margin-top:18px;background:radial-gradient(circle at 50% 20%,rgba(167,139,250,.08),transparent 55%)}.sw-empty>div:first-child{font:700 2rem Syne;color:#a78bfa}.sw-empty b{color:#ddd6fe;font:700 1rem Syne}.sw-empty p{max-width:295px;margin:0;font-size:.78rem;line-height:1.6}.sw-spinner{width:30px;height:30px;border:3px solid rgba(167,139,250,.2);border-top-color:#a78bfa;border-radius:50%;margin:auto;animation:spin .8s linear infinite}@keyframes spin{to{transform:rotate(360deg)}}.sw-scenes{display:grid;gap:11px;padding-top:18px}.sw-scene{position:relative;display:grid;grid-template-columns:22px 35px 1fr 25px;gap:10px;padding:14px 12px;border:1px solid var(--line);border-radius:12px;background:linear-gradient(100deg,rgba(167,139,250,.05),transparent 30%),#10101a}.sw-drag,.sw-delete{border:0;background:transparent;color:#777288;cursor:grab;font-size:19px}.sw-delete{cursor:pointer;font-size:22px;line-height:1}.sw-number{font:500 .75rem 'DM Mono';color:#a78bfa;padding-top:5px}.sw-scene-top{display:flex;justify-content:space-between;align-items:center;margin-bottom:9px;color:#938fa2;font:500 .63rem 'DM Mono';letter-spacing:.08em}.sw-scene-top label{color:#a6a1b4}.sw-scene-top input{width:40px;border:0;border-bottom:1px solid #625d70;background:transparent;color:#ddd6fe;text-align:right;font:inherit;outline:0}.sw-edit{display:block;margin-bottom:8px}.sw-edit span{display:block;margin-bottom:5px;font:700 .61rem 'DM Mono';letter-spacing:.08em}.sw-edit textarea{display:block;resize:vertical;min-height:53px;padding:8px 9px;font-size:.78rem;line-height:1.52}.sw-edit.narration span{color:#ddd6fe}.sw-edit.visual span{color:#8dd8ca}.sw-edit.visual textarea{background:rgba(45,212,191,.035);border-color:rgba(45,212,191,.18);color:#b7ded8}.sw-caption{display:flex;gap:7px;align-items:flex-start;color:#aaa6b4;font-size:.72rem;line-height:1.45}.sw-caption span{padding:2px 4px;border-radius:4px;background:rgba(255,255,255,.08);font:500 .58rem 'DM Mono';color:#e9e4ff}.sw-add{display:block;margin:15px auto 0;background:transparent;padding:9px 14px}.is-locked{opacity:.52;pointer-events:none}@media(max-width:800px){.sw-shell{grid-template-columns:1fr}.sw-hero{padding-top:50px}.sw-canvas{padding:16px}.sw-canvas-head{display:block}.sw-export{margin-top:14px}.sw-scene{grid-template-columns:16px 25px 1fr 19px;padding:12px 8px}.sw-grid{grid-template-columns:1fr}.sw-header{padding:0 16px}.sw-brand i{display:none}}`;

const advancedStyles = `.sw-advanced{margin:2px 0 15px;padding:12px;border:1px solid var(--line);border-radius:10px;background:rgba(255,255,255,.018)}.sw-advanced summary{display:flex;justify-content:space-between;cursor:pointer;list-style:none;color:#c8c5d3;font:700 .7rem 'DM Mono';letter-spacing:.05em}.sw-advanced summary::-webkit-details-marker{display:none}.sw-advanced summary span{color:#c4b5fd;font-size:1rem;line-height:.7}.sw-advanced[open] summary span{transform:rotate(45deg)}.sw-advanced>p{margin:9px 0 13px;color:var(--m);font-size:.72rem;line-height:1.5}.sw-advanced .sw-field{margin-bottom:10px}.sw-advanced .sw-grid{gap:8px}`;

const workspaceStyles = `.sw-root{background:radial-gradient(900px 450px at 88% 3%,rgba(124,58,237,.2),transparent 68%),radial-gradient(760px 380px at 2% 42%,rgba(77,64,160,.12),transparent 72%),#09090f}.sw-hero{padding-bottom:42px}.sw-shell{position:relative;align-items:start;gap:20px;padding-bottom:96px}.sw-panel,.sw-canvas{border-color:rgba(196,181,253,.14);box-shadow:0 24px 60px rgba(0,0,0,.28)}.sw-panel{position:sticky;top:20px;padding:0;overflow:hidden;background:linear-gradient(180deg,rgba(28,27,42,.98),rgba(15,15,24,.98))}.sw-panel-head{margin:0;padding:20px;border-bottom:1px solid var(--line);background:linear-gradient(110deg,rgba(167,139,250,.13),transparent 72%)}.sw-panel-head>span{width:34px;height:34px;border:1px solid rgba(196,181,253,.2);box-shadow:inset 0 1px rgba(255,255,255,.12)}.sw-panel-head b{display:block;font-size:1rem;letter-spacing:-.02em}.sw-panel-head small{margin-top:4px}.sw-control-group{position:relative;margin:0 14px;padding:18px 4px 3px}.sw-control-group:last-of-type{padding-bottom:12px}.sw-group-label{display:flex;align-items:center;gap:8px;margin-bottom:14px;color:#c7beeb;font-size:.63rem}.sw-group-label:after{content:'';height:1px;flex:1;background:linear-gradient(90deg,rgba(196,181,253,.16),transparent)}.sw-field{margin-bottom:16px}.sw-field>span{color:#d4d0df}.sw-field textarea,.sw-field select,.sw-field input,.sw-edit textarea{border-color:rgba(255,255,255,.1);background:rgba(8,8,15,.72);box-shadow:inset 0 1px rgba(255,255,255,.025)}.sw-field textarea,.sw-field select,.sw-field input{transition:border-color .18s,background .18s,box-shadow .18s}.sw-field textarea:focus,.sw-field select:focus,.sw-field input:focus,.sw-edit textarea:focus{border-color:rgba(167,139,250,.78);background:#12111e;box-shadow:0 0 0 3px rgba(139,92,246,.14)}.sw-field select,.sw-field input{height:44px}.sw-word-target{display:grid;grid-template-columns:minmax(0,1fr) auto auto;gap:8px;align-items:center}.sw-word-target input{min-width:0}.sw-word-target b{color:#b8b2c7;font:700 .68rem 'DM Mono';letter-spacing:.04em}.sw-word-target button{border:1px solid rgba(196,181,253,.2);border-radius:8px;padding:8px 9px;background:rgba(167,139,250,.1);color:#ddd6fe;font:700 .64rem 'DM Mono';cursor:pointer;transition:background .18s,border-color .18s}.sw-word-target button:hover{border-color:rgba(196,181,253,.48);background:rgba(167,139,250,.2)}.sw-pills{gap:6px}.sw-pills button{min-width:40px;padding:8px 9px;border-color:rgba(255,255,255,.1);background:rgba(8,8,15,.6)}.sw-pills button.active{box-shadow:0 5px 13px rgba(124,58,237,.18)}.sw-advanced{margin:2px 18px 16px;border-color:rgba(255,255,255,.1);background:rgba(255,255,255,.025)}.sw-tries{margin:0 18px 12px}.sw-notice,.sw-error{margin-left:18px;margin-right:18px}.sw-generate{width:calc(100% - 36px);margin:0 18px 18px;padding:14px;border-radius:11px;box-shadow:0 11px 26px rgba(124,58,237,.3)}.sw-canvas{min-height:680px;padding:0;overflow:hidden;background:linear-gradient(145deg,rgba(24,23,36,.98),rgba(14,14,23,.98))}.sw-canvas-head{align-items:flex-start;padding:24px 26px 20px;background:linear-gradient(105deg,rgba(167,139,250,.1),transparent 46%)}.sw-canvas h2{font-size:1.5rem}.sw-kicker{color:#c7beeb}.sw-export button,.sw-add{border-color:rgba(196,181,253,.18);background:rgba(167,139,250,.1);border-radius:9px;padding:9px 11px}.sw-export button:hover,.sw-add:hover{box-shadow:0 7px 18px rgba(124,58,237,.16)}.sw-empty{min-height:490px;margin:22px 26px;border-color:rgba(196,181,253,.2);background:radial-gradient(circle at 50% 22%,rgba(167,139,250,.14),transparent 42%),linear-gradient(135deg,rgba(255,255,255,.025),transparent)}.sw-empty>div:first-child{display:grid;place-items:center;width:54px;height:54px;margin:0 auto 3px;border:1px solid rgba(196,181,253,.25);border-radius:17px;background:rgba(167,139,250,.11);box-shadow:0 10px 30px rgba(124,58,237,.14)}.sw-empty b{font-size:1.08rem}.sw-scenes{padding:20px 26px 4px;gap:13px}.sw-scene{padding:16px 14px;border-color:rgba(255,255,255,.1);border-radius:14px;background:linear-gradient(110deg,rgba(167,139,250,.09),transparent 28%),rgba(12,12,20,.72);transition:border-color .18s,transform .18s,box-shadow .18s}.sw-scene:hover{border-color:rgba(167,139,250,.32);box-shadow:0 12px 25px rgba(0,0,0,.16);transform:translateY(-1px)}.sw-scene-top{margin-bottom:11px}.sw-edit textarea{border-radius:9px}.sw-caption{padding:8px 9px;border-left:2px solid rgba(167,139,250,.42);border-radius:0 7px 7px 0;background:rgba(255,255,255,.025)}.sw-add{margin:17px auto 24px}@media(max-width:800px){.sw-panel{position:static}.sw-shell{gap:14px;padding-bottom:72px}.sw-panel-head{padding:18px}.sw-canvas-head{padding:20px}.sw-empty{min-height:360px;margin:16px}.sw-scenes{padding:16px}.sw-scene{padding:13px 10px}.sw-generate{margin-bottom:18px}}@media(max-width:390px){.sw-word-target{grid-template-columns:1fr auto}.sw-word-target button{grid-column:1/-1}}`;

const sceneEditorStyles = `.sw-tries strong{display:block;font-size:.75rem;letter-spacing:.02em}.sw-tries small{display:block;margin-top:4px;color:#aaa4bc;font:500 .62rem 'DM Mono'}.sw-tries.green small{color:#8de6bd}.sw-tries.yellow small{color:#f6d878}.sw-tries.red small{color:#f2a5b1}.sw-word-warning{margin:9px 0 0;padding:9px 10px;border:1px solid rgba(251,191,36,.3);border-radius:8px;background:rgba(251,191,36,.08);color:#fde68a;font-size:.71rem;line-height:1.5}.sw-scene{grid-template-columns:22px 35px minmax(0,1fr)}.sw-scene-top{gap:10px}.sw-scene-meta{display:flex;align-items:center;gap:10px}.sw-scene-meta>b{color:#aaa4bc;font:500 .61rem 'DM Mono';letter-spacing:.04em;white-space:nowrap}.sw-scene-meta label{white-space:nowrap}.sw-scene-footer{display:flex;align-items:flex-start;justify-content:space-between;gap:14px;margin-top:2px}.sw-scene-footer .sw-caption{flex:1;min-width:0}.sw-scene-actions{display:flex;gap:6px}.sw-scene-actions button{border:1px solid rgba(196,181,253,.18);border-radius:7px;padding:7px 8px;background:rgba(255,255,255,.035);color:#d9d4e8;font:700 .61rem 'DM Mono';cursor:pointer;transition:background .18s,border-color .18s,color .18s}.sw-scene-actions button:hover{border-color:rgba(196,181,253,.5);background:rgba(167,139,250,.15);color:#fff}.sw-scene-actions .danger{color:#f3a9b6}.sw-scene-actions .danger:hover{border-color:rgba(248,113,113,.45);background:rgba(248,113,113,.12)}@media(max-width:800px){.sw-scene{grid-template-columns:16px 25px minmax(0,1fr)}}@media(max-width:560px){.sw-scene-top{align-items:flex-start;flex-direction:column}.sw-scene-footer{align-items:stretch;flex-direction:column}.sw-scene-actions{justify-content:flex-end}.sw-scene-actions button{padding:8px 10px}}`;
